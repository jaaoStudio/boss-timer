# Artale Boss Timer

一個讓楓之谷 Artale 玩家協同追蹤 Boss 重生時間的即時工具。玩家自行建立房間、邀請隊友加入，在遊戲中擊殺 Boss 後回報，系統自動計算該頻道的重生區間並同步給所有房間成員。

## Language

**Room（房間）**：
玩家自建的協作追蹤空間。成員自由加入，共享同一份 Boss 擊殺紀錄。Room 是由玩家自組的，與遊戲伺服器本身無直接對應關係。
_Avoid_：會話、群組、隊伍。Room 超過 7 天無活動後永久失效，ID 不重複使用，玩家需建立新房間。

**Channel（頻道）**：
遊戲內的平行世界實例。同一張地圖在不同頻道完全獨立，各 Boss 有各自的重生計時器。相同 Boss 種類的最大/最小重生時間固定，但每個頻道的計時器互不影響。
_Avoid_：伺服器、實例

**BossRecord（紀錄）**：
玩家對某個頻道上某隻 Boss 狀態的一次觀察回報。系統信任回報者，不驗證遊戲內的實際狀態。同一房間內，同一頻道同一 Boss 種類的**最新一筆紀錄**決定當前狀態。
_Avoid_：事件、擊殺紀錄（因為不一定是擊殺）

**回報狀態（Reported Status）**：
玩家按下按鈕時選擇的觀察值。有三種：`killed`（親手擊殺）、`alive`（確認 Boss 存活）、`not_found`（親自前往該頻道但未看見 Boss，無從推測是否在重生中）。
_Avoid_：狀態（太模糊，與「計算狀態」衝突）

**計算狀態（Current Status）**：
系統根據最新紀錄與當前時間自動推算的 Boss 即時狀態。`respawning`（確定在重生區間內）、`may_respawn`（已過最小重生時間，Boss 可能已刷新）、`alive`（已超過最大重生時間，推測已刷新）、`not_found`（直接沿用回報狀態，無法推算）。
_Avoid_：狀態（太模糊）

**BossType（Boss 種類）**：
定義一種 Boss 的模板，包含名稱與重生時間參數。分兩種：**全域 Boss**（`room_id = null`，由系統預設，所有房間共用）、**自訂 Boss**（`room_id` 有值，由房間成員自行建立）。自訂 Boss 的設計意圖是讓玩家因應未來新增的 Boss，或讓其他遊戲的玩家將此系統改作通用 Boss 計時器。
_Avoid_：Boss 類型、Boss 模板

**Respawn Window（重生區間）**：
Boss 被擊殺後的隨機刷新時間範圍，由 `respawn_min_time`（最早可能刷新）到 `respawn_max_time`（必定已刷新）構成。區間結束後 Boss 必定存在於頻道中。

**推薦頻道（Recommended Channels）**：
值得玩家前往查看的頻道清單。收錄計算狀態為 `may_respawn`（已過最小重生時間）及 `alive`（已超過最大重生時間）的頻道。後者因 `respawn_min_time` 為 null，排序時自動排在最前面（確定有 Boss 的頻道優先）。頻道在紀錄超過 2 天後自動從房間狀態中消失。
_Avoid_：優先頻道

**避免頻道（Avoid Channels）**：
確定仍在重生區間內（`respawning`）的頻道清單，提示玩家不必前往以節省時間。

**匿名使用者（Anonymous User）**：
未登入的使用者，由系統分配一個 UUID 作為識別，可自行設定暱稱（預設「一個路人」）。可執行幾乎所有功能（回報紀錄、建立房間、管理自訂 Boss）。唯一限制：無法隱藏歷史紀錄區塊，偏好設定僅存於 localStorage，無法跨裝置同步。

**已登入使用者（Authenticated User）**：
透過 Google OAuth 登入的使用者。與匿名使用者功能相同，但額外擁有：可隱藏歷史紀錄區塊（`showRecordHistory` 偏好）、偏好設定存於資料庫並跨裝置同步。

## Example Dialogue

> **玩家 A**：我剛在頻道 5 打死黑龍了，已經回報了。  
> **玩家 B**：好，我在推薦頻道有看到頻道 5。什麼時候可以去刷？  
> **玩家 A**：重生區間是 30 到 50 分鐘，現在顯示 `may_respawn`，你可以去碰碰運氣。  
> **玩家 B**：等等，我剛去頻道 5 看，Boss 是活的，我補一筆紀錄。  
> **玩家 A**：收到，系統現在會以你那筆為準。  

> **開發者 A**：同一個 Room 裡，頻道 5 的黑龍有兩筆紀錄——哪一筆算當前狀態？  
> **開發者 B**：最新那筆，不管是誰回報的。  

## Flagged Ambiguities

**`alive` 狀態未出現在推薦頻道**：玩家確認 `alive`（超過最大重生時間）的頻道應納入推薦頻道，但目前前端 `priorityChannels` getter 只篩選 `may_respawn`。