# 過期房間 ID 永久退役，不重複使用

Room 超過 7 天無活動後標記為 `is_active = false`。此後該 Room ID 永久從可用池中移除——ID 唯一性檢查不過濾 `is_active`，已存在資料庫的 ID 皆不重用。

玩家嘗試加入過期房間時，系統回傳「房間不存在」（`get_room_by_id` 只查 `is_active = true`），不區分「從未存在」與「已過期」兩種情況。

Room ID 空間為 31 位字元集取 10 碼（約 8.2 × 10¹⁴ 組合），耗盡不是實際考量。